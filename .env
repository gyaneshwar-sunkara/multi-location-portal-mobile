# API URL — required in production, optional in development (falls back to localhost)
# For physical device testing, use your machine's LAN IP (e.g., http://192.168.1 .100:3000/api/v1)
EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1